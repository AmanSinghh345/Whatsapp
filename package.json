{
  "name": "whatsapp-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "npm -ws run typecheck",
    "lint": "npm -ws run lint",
    "format": "npm -ws run format"
  }
}

